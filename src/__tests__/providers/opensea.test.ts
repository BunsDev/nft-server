import { OpenSea as OpenSeaMarketConfig } from "../markets";
import { OpenSeaProvider } from "../markets/OpenSeaProvider";

describe(`OpenSeaProvider tests`, async () => {
  let OSProvider;
  beforeEach(() => {
    OSProvider = new OpenSeaProvider(OpenSeaMarketConfig);
  });

  

});

